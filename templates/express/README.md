# Archived Express Template Notes

This folder is kept only for compatibility with older repository links.

Use `templates/express-firewall` for the current Express example. The current package does not ship the older proxy-style middleware or dashboard/budget-management CLI features.
